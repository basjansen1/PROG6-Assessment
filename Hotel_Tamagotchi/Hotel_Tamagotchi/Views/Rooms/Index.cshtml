@model IEnumerable<Hotel_Tamagotchi.Models.ViewModels.RoomViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Room.Size)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Room.Type)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Room.Size)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Room.Type)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Room.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Room.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Room.ID })
        </td>
    </tr>
}

</table>
